'use strict';
(function () {
  /*var searchLink = document.querySelector('.search');
  var searhPopup = document.querySelector('.serch__popup');

  searchLink.addEventListener('click', function (evt) {
    evt.preventDefault();
    if (!searchLink.classList.contains('search--opened')) {
      searchLink.classList.add('search--opened');
      searhPopup.classList.add('search__popup--opened');
    }
    if (searchLink.classList.contains('search--opened') && ) {

    }
  });*/
//keypress + mouseover/mouseout

  //empty -> close; + escape + click outside
  //full -> search; + enter
})();
